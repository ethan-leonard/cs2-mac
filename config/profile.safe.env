#!/usr/bin/env bash
# Conservative defaults for normal CS2 play.
# These are loaded by the launcher scripts with `set -a`, so plain assignments
# and explicit exports both work.

export WINEMSYNC=1
export WINEESYNC=1
export DXVK_ASYNC=0
export MTL_HUD_ENABLED=0
export WINEDEBUG=-all
